# RFC 0001: Context Parallelism 仿真支持

> **状态**: Draft v2（修正版）  
> **作者**: System  
> **日期**: 2026-05-09  
> **目标项目**: `ascend_msmodeling`  
> **参考实现**: `vllm-ascend` (`vllm_ascend/attention/context_parallel/`, `vllm_ascend/distributed/parallel_state.py`)

---

## 1. 背景与动机

### 1.1 CP 并行概述

Context Parallelism (CP) 是处理长序列推理的关键并行策略。当单张卡无法容纳一个请求的全部 KV Cache 时，CP 将序列按 token 维度切分到多个设备上，每张卡只存储部分 KV，注意力计算时通过集合通信获取完整 KV 再计算。

vllm-ascend 在 `vllm_ascend/attention/context_parallel/` 下实现了完整的 CP 方案，分为两层：
- **PCP**（Prefill Context Parallelism）：prefill 阶段，长序列的 Q 按 head/tail 切分后各自做 attention，最后通过 LSE 加权合并
- **DCP**（Decode Context Parallelism）：decode 阶段，KV cache 分布在多卡，每个 rank 只计算自己负责的那部分 KV 对应的 attention
- PCP 和 DCP 使用**独立的通信组**：`get_pcp_group()` 和 `get_dcp_group()`

当前 `ascend_msmodeling` 已具备 TP/DP/EP/PP 及 Ulysses 序列并行的仿真能力（`tensor_cast/model_config.py` `ParallelConfig`，`tensor_cast/parallel_group.py`），但**缺少 CP 并行的仿真支持**。本 RFC 设计如何将 CP 引入仿真框架，并支持在不同推理引擎的 CP 实现间切换。

### 1.2 参考实现：vllm-ascend CP 架构

#### 1.2.1 Rank 布局

vllm-ascend 的分布式 rank 布局（`parallel_state.py`）：

```python
# 实际 rank 网格: [ExternalDP, DP, PP, PCP, TP]
all_ranks = torch.arange(world_size).reshape(
    -1, global_dp_size, global_pp_size, global_pcp_size, global_tp_size
)
```

**注意**：vllm-ascend 的 rank 网格**没有独立的 EP 维度**；EP 等价分组通过 `transpose` 从该网格派生。`global_pcp_size` 来自 `parallel_config.prefill_context_parallel_size`。

#### 1.2.2 CP 通信流

**Prefill (PCP) 流程**：

```
Step 0: all_gather KV cache 跨 PCP/DCP group → 每个 rank 获得完整 KV（gather_kv_cross_cp）
Step 1: Q 按 head/tail 索引切分（q_head_idx / q_tail_idx，由 pcp_utils 预计算）
Step 2: head 部分做 attention（无 mask）、tail 部分做 attention（有 causal mask）
Step 3: all_gather attn_output + softmax_lse 跨 PCP group
Step 4: npu_attention_update(LSE 加权合并) → 最终输出
```

**Decode (DCP) 流程**：

```
Step 0: all_gather KV cache 跨 DCP group → 每个 rank 获得完整 KV
Step 1: 各 rank 做 decode attention（在完整 KV 上）
Step 2: attn_out_lse 做 permute + all_to_all_single → 重排 heads 到正确 rank
Step 3: npu_attention_update 合并 → 最终输出
```

PCP 和 DCP 可以**同时启用**（PCP 负责 prefill 阶段，DCP 负责 decode 阶段），最终通过 `_process_attn_out_lse()` 统一处理。

#### 1.2.3 关键通信原语（`common_cp.py`）

| 函数 | 作用 | 阶段 |
|------|------|------|
| `get_pcp_group().all_gather()` | PCP 组内聚合 attn_out + LSE | Prefill |
| `get_pcp_group().all_gather()` | KV cache 聚合（gather_kv_cross_cp） | Prefill (step 0) |
| `get_dcp_group().all_gather()` | KV cache 聚合 | Decode (step 0) |
| `dist.all_to_all_single(group=dcp_group)` | DCP 组内重排 heads | Decode |
| `torch_npu.npu_attention_update()` | LSE 加权合并多个 attention 输出 | 两者 |
| `_update_out_and_lse()` | 纯 Python 实现的 LSE merge（无 NPU 依赖） | fallback |

#### 1.2.4 Attention 类型适配

| 文件 | 类型 | 适用模型 | CP 差异 |
|------|------|----------|---------|
| `attention_cp.py` | 标准 Flash Attention | Llama、Qwen 等 | head/tail split + LSE merge |
| `sfa_cp.py` | Sparse Flash Attention | 使用 SFA 的模型 | 额外处理 sparse mode 参数 |
| `mla_cp.py` | Multi-head Latent Attention | DeepSeek-V2/V3 | 额外包含 `kv_b_proj` + `kv_tail_proj` 索引 |

#### 1.2.5 Chunked Prefill + CP

vllm-ascend 通过 `CPChunkedContextMetadata` 和 `AscendMetadataForPrefill.ChunkedContextMetadata` 处理 chunked prefill 与 CP 的交互：

```python
# chunked prefill 中，每个 PCP rank 的 chunk 长度独立追踪
local_context_lens_allranks:  # [num_reqs, pcp_size, dcp_size]
actual_seq_lengths_kv = cumsum(local_chunked_kv_lens_rank)
```

这对仿真很重要：如果 chunked prefill 启用且 CP>1，每个 chunk 只包含该 rank 负责的 token 子集，通信量按实际 token 数计算。

---

## 2. ascend_msmodeling 现状分析

### 2.1 现有并行配置

`tensor_cast/model_config.py` 的 `ParallelConfig` 已有：

| 字段 | 说明 |
|------|------|
| `tensor_parallel_size` | TP size |
| `data_parallel_size` | DP size（由 world_size // tp // pp 推导） |
| `pipeline_parallel_size` | PP size |
| `expert_parallel_size` | EP size |
| `ulysses_size` | Ulysses 序列并行 size（默认 1） |

**`world_size` 约束（`__post_init__`）**：
```python
tp_size * dp_size * pp_size == world_size
```
注意：`ep_size` 不在基础 world_size 约束中——EP 作用于 MoE 层，沿用独立约束 `moe_tp * moe_dp * ep == world_size`。

加入 CP 后需要扩展为：
```python
tp_size * dp_size * pp_size * cp_size == world_size
```

同时需要修改 `dp_size` 的推导逻辑：
```python
if self.data_parallel_size is None:
    self.data_parallel_size = (
        self.world_size
        // self.tensor_parallel_size
        // self.pipeline_parallel_size
        // self.context_parallel_size
    )
```

### 2.2 现有 rank 布局

`tensor_cast/parallel_group.py` 的 `initialize_model_parallel`：

```python
rank_groups_raw = all_ranks.reshape(
    -1, data_parallel_size, pipeline_parallel_size,
    expert_parallel_size, tensor_parallel_size
)  # [ExternalDP, DP, PP, EP, TP]
```

`ParallelGroupType` 枚举缺少 `CONTEXT_PARALLEL`。

### 2.3 与 Ulysses 的关系

ascend_msmodeling 已有 `ulysses_size`（`ParallelConfig`），Ulysses 和 CP 是两种**互斥**的长序列并行策略：

| | Ulysses | CP |
|------|---------|------|
| 切分维度 | head 维度 | token 维度 |
| 通信原语 | all-to-all | all-gather (PCP) / all-to-all-single (DCP) |
| 适用阶段 | prefill | prefill + decode |
| 对 attention 计算影响 | 每个 rank 计算全部 token × 部分 head | 每个 rank 计算部分 token × 全部 head |

仿真框架应支持**二选一**：`ulysses_size > 1` 时 CP 无效，反之亦然。实现时在 `ParallelConfig.__post_init__` 中增加互斥检查。

### 2.4 通信建模仿真基础设施

`serving_cast/communication.py` 已有 `Channel` 和 `CommunicationManager`，支持：
- `host2device_sync/async`
- `device2device_sync/async`

CP 仿真可直接复用 `CommunicationManager.device2device_sync` 建模 all-gather / all-to-all 通信开销。

`tensor_cast/device.py` 中还有 `InterconnectTopology` 类，支持更精细的互联拓扑（NVLink/RoCE）建模。CP 仿真中可根据 `InterconnectTopology` 估计不同 CP size 下的通信带宽。

---

## 3. Work Unit 分解

### Unit 1: 扩展 ParallelConfig 和 ParallelGroup 支持 CP

| 字段 | 内容 |
|------|------|
| **id** | `unit-001-cp-config-and-group` |
| **depends_on** | 无 |
| **scope** | `tensor_cast/model_config.py`（`ParallelConfig`），`tensor_cast/parallel_group.py`（`ParallelGroupType`、`ParallelGroupManager`） |
| **acceptance_tests** | `cp_group.world_size == cp_size`；rank 正确归入对应 CP group；`ulysses_size>1` 与 `cp_size>1` 互斥报错 |
| **risk_level** | Tier 1（基础层，影响所有后续 Unit） |
| **rollback_plan** | 移除 `cp_size` / `CONTEXT_PARALLEL` / `cp_group` |

**实施要点**：

1. `ParallelConfig` 新增字段：
```python
context_parallel_size: int = 1       # CP size（含 PCP 和 DCP 的统一 group）
prefill_context_parallel_size: int = 1  # 可选的 PCP size（≤ context_parallel_size）
decode_context_parallel_size: int = 1   # 可选的 DCP size（≤ context_parallel_size）
```

2. `__post_init__` 修改 world_size 约束和 dp_size 推导：
```python
def __post_init__(self):
    if self.context_parallel_size <= 0:
        raise ValueError("context_parallel_size must be >= 1")
    if self.ulysses_size > 1 and self.context_parallel_size > 1:
        raise ValueError(
            "Ulysses (ulysses_size) and Context Parallelism (cp_size) "
            "are mutually exclusive — choose one."
        )
    # 扩展 world_size 约束
    if self.tensor_parallel_size * self.data_parallel_size \
       * self.pipeline_parallel_size * self.context_parallel_size \
       != self.world_size:
        raise ValueError(...)
```

3. `ParallelGroupType` 新增：
```python
CONTEXT_PARALLEL = "context_parallel"
```

4. rank 网格改为（在 EP 之前插入 CP）：
```python
rank_groups_raw = all_ranks.reshape(
    -1, dp_size, pp_size, cp_size, ep_size, tp_size
)
```

5. `ParallelGroupManager` 新增 `self.cp_group` 成员（**只有这一个 CP group**，没有 `o_proj_cp_group` 或 `mlp_cp_group`——CP 仅作用于 attention 计算，不切分 Linear/proj/MLP 权重）。

### Unit 2: CP 通信建模仿真

| 字段 | 内容 |
|------|------|
| **id** | `unit-002-cp-comm-modeling` |
| **depends_on** | `unit-001` |
| **scope** | 新增 `tensor_cast/cp_sim.py` |
| **acceptance_tests** | all_gather 开销随 cp_size 和 seq_len 线性增长；PCP 和 DCP 的通信量不同；chunked prefill 下通信量按实际 token 数计算 |
| **risk_level** | Tier 2 |
| **rollback_plan** | 删除 `cp_sim.py` |

**实施要点**：

```python
@dataclass
class CPSimResult:
    """CP 仿真结果"""
    pcp_comm_time_s: float   # PCP 通信耗时
    pcp_comp_time_s: float   # PCP 计算耗时（attention_update merge）
    dcp_comm_time_s: float   # DCP 通信耗时
    dcp_comp_time_s: float   # DCP 计算耗时

def simulate_cp_overhead(
    seq_len: int,
    query_len: int,
    head_dim: int,
    num_heads: int,
    num_kv_heads: int,
    cp_size: int,
    dcp_size: int,
    kv_cache_dtype: torch.dtype,
    attn_type: str,             # "flash_attn" | "sfa" | "mla"
    cp_engine: str,             # "vllm_ascend" | "sglang" | "custom"
    interconnect: InterconnectTopology,
    is_chunked_prefill: bool = False,
) -> CPSimResult:
    """
    模拟 CP 通信 + 计算开销。
    
    通信建模：
      - KV all_gather (step 0): all_gather(KV_cache, dim=0) 跨 CP group
        * 数据量 = seq_len * num_kv_heads * head_dim * 2 * kv_cache_dtype.itemsize
        * SFA/MLA 显式执行 all_gather；标准 attention CP 用 index_select 等价访问
      - PCP: all_gather(attn_out + softmax_lse, dim=0)
        * 单 rank 数据量 = seq_len/cp_size * num_heads * (head_dim + 1) * float32
      - DCP: all_to_all_single(attn_out_lse, group=dcp_group)
        * 数据量 = batch_size * num_heads * (head_dim + 1) * float32
        * 注意 decode 阶段 batch_size 通常较小，dcp_size 默认等于 cp_size
    
    计算建模：
      - attention_update (LSE merge): O(seq_len * num_heads) 的 log/exp/sum 运算
      - vllm-ascend 使用 NPU 融合算子；sglang/custom 使用 Python 实现
    
    MLA 附加开销：
      - kv_b_proj: 额外的投影计算
      - kv_tail_proj: tail 部分 KV 的额外索引操作
    """
```

**三种引擎的 CP 通信模式差异**：

| 引擎 | PCP 通信 | DCP 通信 | LSE merge |
|------|----------|----------|-----------|
| `vllm_ascend` | all_gather KV + all_gather attn_out (PCP) | all_gather KV + all_to_all_single (DCP) | `npu_attention_update` (融合) |
| `sglang` | ring send/recv KV chunk | 不支持（或仅 PCP） | softmax recomputation |
| `custom` | 用户指定 | 用户指定 | 用户指定 |

**Chunked Prefill 下的特殊处理**：

当 `is_chunked_prefill=True` 且 `cp_size > 1` 时：
- 每个 chunk 的 token 数 = `seq_len / cp_size / num_chunks`
- all_gather 的数据量基于该 chunk 的实际 token 数，而非完整 seq_len
- chunk 边界处需要额外处理 `ChunkedContextMetadata` 的 `local_context_lens_allranks`

### Unit 3: CP 仿真配置系统

| 字段 | 内容 |
|------|------|
| **id** | `unit-003-cp-config-system` |
| **depends_on** | `unit-001`, `unit-002` |
| **scope** | `tensor_cast/model_config.py`（新增 `CPConfig`），`serving_cast/config.py`（集成） |
| **acceptance_tests** | 配置切换后 CP 参数正确传递；引擎切换后通信模式变化正确；非法配置回退到单卡 |
| **risk_level** | Tier 2 |
| **rollback_plan** | 移除 `CPConfig`，恢复默认 `cp_size=1` |

**配置类设计**：

```python
@dataclass
class CPConfig:
    """Context Parallelism 仿真配置"""
    cp_size: int = 1
    cp_engine: str = "vllm_ascend"        # "vllm_ascend" | "sglang" | "custom"
    attn_type: str = "flash_attn"         # "flash_attn" | "sfa" | "mla"
    cp_kv_cache_interleave_size: int = 128
    enable_pcp: bool = True
    enable_dcp: bool = True
    
    # 引擎特定参数（不干预默认行为，仅 override）
    engine_params: dict = field(default_factory=dict)
    
    def __post_init__(self):
        if self.cp_engine not in ("vllm_ascend", "sglang", "custom"):
            raise ValueError(f"Unknown cp_engine: {self.cp_engine}")
        if self.attn_type not in ("flash_attn", "sfa", "mla"):
            raise ValueError(f"Unknown attn_type: {self.attn_type}")
```

**引擎切换逻辑**：

```python
class CPEngine(ABC):
    """CP 引擎抽象基类"""
    
    @abstractmethod
    def get_pcp_comm_ops(self, params: CPCommParams) -> list[CommOp]:
        """返回 PCP 阶段的通信算子序列"""
        ...
    
    @abstractmethod
    def get_dcp_comm_ops(self, params: CPCommParams) -> list[CommOp]:
        """返回 DCP 阶段的通信算子序列"""
        ...
    
    @abstractmethod
    def get_merge_strategy(self) -> str:
        """返回 LSE merge 策略: "npu_fusion" | "softmax_recompute" | "custom" """
        ...

class VllmAscendCPEngine(CPEngine):
    """
    vllm-ascend 风格：
    PCP: all_gather attn_out + LSE → npu_attention_update
    DCP: all_to_all_single → npu_attention_update
    """
    ...

class SglangCPEngine(CPEngine):
    """
    sglang 风格：
    PCP: ring-based KV chunk 传递 → 独立 attention → softmax recomputation merge
    DCP: 通常不支持
    """
    ...
```

### Unit 4: 集成到 serving_cast 流水线

| 字段 | 内容 |
|------|------|
| **id** | `unit-004-integration` |
| **depends_on** | `unit-003` |
| **scope** | `serving_cast/model_runner.py`, `tensor_cast/core/model_runner.py` |
| **acceptance_tests** | 启用 CP 后 prefill/decode 时间随 cp_size 增加而减少（整体吞吐提升）；batch 混合 prefill+decode 场景正确 |
| **risk_level** | Tier 3（集成复杂度最高） |
| **rollback_plan** | 移除 attention 阶段的 CP 分支，恢复仅 TP/DP 路径 |

**实施要点**：

1. `serving_cast/model_runner.py` 中 `init_tensor_cast_model_runner` 新增 `cp_size` / `cp_config` 参数传递
2. `tensor_cast/core/model_runner.py` 的 attention 阶段调用 `simulate_cp_overhead()`
3. 每个 transformer layer 的 attention 子图增加 CP 通信开销
4. CP 与已有并行的组合约束：
   - `tp_size * dp_size * pp_size * cp_size == world_size`
   - `ulysses_size > 1` 时跳过 CP 仿真

### Unit 5: 端到端验证与 Benchmark

| 字段 | 内容 |
|------|------|
| **id** | `unit-005-e2e-validation` |
| **depends_on** | `unit-004` |
| **scope** | 新增 `tests/test_tensor_cast/test_cp_sim.py` |
| **acceptance_tests** | 128K 长序列下 CP=2/4/8 的吞吐提升曲线合理；三种 attention 类型仿真均可运行；引擎切换功能正常 |
| **risk_level** | Tier 2 |
| **rollback_plan** | 删除测试文件 |

**验证矩阵**：

| 场景 | CP size | attention 类型 | 序列长度 | 验证项 |
|------|---------|---------------|----------|--------|
| Prefill only | 2, 4, 8 | flash_attn, sfa, mla | 32K, 128K | 通信开销占比 ~ 10-30% |
| Decode only | 2, 4, 8 | flash_attn | 128K context | DCP 通信 << PCP |
| Mixed | 2, 4 | flash_attn | 64K | batch 混合正确 |
| Engine switch | 2 | flash_attn | 32K | vllm_ascend vs sglang 输出不同 |
| Ulysses conflict | 2 + ulysses=2 | flash_attn | 32K | 报错拒绝 |

---

## 4. DAG 依赖图

```
unit-001-cp-config-and-group ──┬──► unit-003-cp-config-system ──► unit-004-integration ──► unit-005-e2e-validation
                                │
unit-002-cp-comm-modeling ─────┘
```

`unit-001` 和 `unit-002` **可并行开发**，`unit-003` 依赖两者。

---

## 5. 配置示例

### 5.1 YAML 配置（vllm-ascend 风格）

```yaml
# ascend_msmodeling/configs/cp_vllm_ascend.yaml
parallel_config:
  world_size: 8
  tp_size: 2
  dp_size: 2
  cp_size: 2          # NEW
  pp_size: 1
  ep_size: 1

cp_config:
  cp_engine: "vllm_ascend"
  attn_type: "flash_attn"
  cp_kv_cache_interleave_size: 128
  enable_pcp: true
  enable_dcp: true
```

### 5.2 引擎切换：sglang ring-attention

```yaml
cp_config:
  cp_engine: "sglang"
  attn_type: "flash_attn"
  enable_pcp: true
  enable_dcp: false           # sglang 通常仅支持 prefill CP
  engine_params:
    ring_degree: 4
    chunk_size: 8192
```

### 5.3 MLA 模型 CP（DeepSeek-V3）

```yaml
cp_config:
  cp_engine: "vllm_ascend"
  attn_type: "mla"
  engine_params:
    kv_lora_rank: 512
    qk_rope_head_dim: 64
    v_head_dim: 128
```

### 5.4 仅 PCP、无 DCP 场景

```yaml
cp_config:
  cp_engine: "vllm_ascend"
  attn_type: "flash_attn"
  enable_pcp: true
  enable_dcp: false    # 适用于短 decode、长 prefill 场景
```

---

## 6. 复杂度与风险总结

| 等级 | Work Unit | 关键风险 |
|------|-----------|----------|
| Tier 1 | Unit 1 (Config+Group) | rank 网格 reshape 可能影响已有并行组合，需回归测试 |
| Tier 2 | Unit 2 (Comm Modeling) | all_gather / all_to_all 开销估计依赖 device2device 带宽模型精度；MLA 仿真复杂度高 |
| Tier 2 | Unit 3 (Config System) | 多引擎抽象层增加维护成本；引擎间的 LSE merge 策略差异需精确建模 |
| Tier 3 | Unit 4 (Integration) | CP + TP + DP + chunked prefill 组合爆炸，需仔细验证边界条件 |
| Tier 2 | Unit 5 (Validation) | 缺乏真实硬件 CP 数据做校准（可先用 analytical 估计） |

---

## 7. 开放问题

1. **PD 分离**：vllm-ascend 支持 Prefill/Decode 分离部署（`kv_transfer` 子系统），CP 在 PD 场景下 KV cache 需要跨 P 和 D 节点传输。当前 RFC 暂不覆盖 PD 分离场景，留待后续 RFC。

2. **CP + EP 组合**：vllm-ascend 的实际 rank 网格不含 EP 维度（EP 通过 transpose 派生）。ascend_msmodeling 是否需要在 rank 布局中同时包含 CP 和 EP，取决于是否计划同时使用 MoE 和长序列。

3. **真实数据校准**：仿真精度依赖 `InterconnectTopology` 的带宽参数和 `simulate_cp_overhead` 的计算模型。建议后续用 vllm-ascend 真实 profiling 数据校准。

---

## 8. 输出物

- [ ] RFC execution log — 本文档
- [ ] Unit scorecards — 各 unit 的测试通过/失败统计
- [ ] Dependency graph snapshot — 见第 4 节
- [ ] Integration risk summary — 见第 6 节
